[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:BuildBackend"

[project]
name = "embeder"
version = "0.1.0"
dependencies = ["sentence-transformers"]

[project.scripts]
embeder = "embeder.server:main"
